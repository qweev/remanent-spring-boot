version: '3.6'

services:
  db:
    image: mysql:5.7
    #@volumes:
    #  - db_data:/var/lib/mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: sklep
      MYSQL_USER: sklep
      MYSQL_PASSWORD: sklep

  my-sql-adminer:
    image: adminer
    ports:
      - 8087:8080